# J–V 曲线显示评审 · 证据图

2026-09-06。J–V 图在四种真实工况下渲染出来，量过像素和数值之后拍的标注图。
判断依据全在图上，正文只放数字和位置。

怎么复现：`python -m bace.service --sim --fast --port 8900 --ui ui/`，把 `ui/fixtures/jv_sim.json`
喂给 `jvModel`，在 Chromium 里量 `getBoundingClientRect()`。图上的每个数字都是量出来的，不是估的。

**先读 [`benchmark.md`](benchmark.md)** —— 参照物是 LabVIEW Graph 的四件运行期控件，
issue 按能力排，不按单个毛病排。`b1.png` 是控件对照，`b2.png` 是 issue 归属。

| 图 | 说的是 | issue |
|---|---|---|
| `f1` | J–V 图被一个空的 172 px 列吃掉 33 % 宽度 | #64 |
| `f2` | 轴标着 `\|I\| / A`，脚注却说 `\|J\|` | #65 |
| ~~`f3`~~ | ~~双向扫描：正扫和回扫在指标表和光标读数里同名~~ | #66 · 已关闭 |
| ~~`f4`~~ | ~~单光强的双向扫，被描述成一段光强 ramp~~ | #67 · 已关闭 |
| `f5` | 同一张卡上，负号有两个字形 | #68 |
| `f6` | 卡片标题写「1 curves」 | #69 |
| `f7` | 纵轴窗口有一半高度是空的 | #70 |
| `f8` | y 有网格线，x 一条都没有；y 也只有 3 条 | #71 |
| `f9` | 暗扫对数图套用了光扫的 2.6:1，设计稿是 1.53:1 | #72 |
| `f10` | 对数 `\|J\|` 把符号毁掉了：过零点变成一串噪声毛刺 | #73 |
| `f11` | 曲线超过四条，身份只剩图例一条路 | #74 |
| `f12` | 图上没有 V_oc / J_sc / MPP | #75 |
| `f13` | 图上没有扫描速率 | #76 |
| `f14` | 暗扫的指标表是一张空表 | #77 |
| `f15` | 主科学图形是唯一不能导出的那个 | #78 |
| `f16` | 图上没有一样东西是可调的 | #79 |
| `f17` | `ui-rules` §4「−4 V 起点是版面问题」只做到了「没裁」 | #80 |

红 = 现在的行为，绿 = 建议或参照物（设计稿 / 已有能力）。

**2026-09-07 · 范围收窄。** owner 把迟滞移出范围，`both_directions` 一并不考虑：
#66、#67 关闭（两个都只有开 `both_directions` 才出现）；#76 只剩扫描速率；#74 按不开双向重量过
（图例 11 → 6 条，读数 280 → 155 字符），「起点标注压字」撤回 —— 4 条曲线时标签间距 36–46 px，本来就不碰。

**2026-09-07 · 按 benchmark 重排。** 见 [`benchmark.md`](benchmark.md)。参照物是 LabVIEW Graph，
issue 从「一个毛病一条」改成「一项能力一条」：

| | issue | 吸收了 |
|---|---|---|
| ① 坐标轴可控（Scale Legend + 双击改 min/max + lin/log + 网格） | **#70** 核心 | #71 #72 的一部分、#79 的 log/裁剪 |
| ② 缩放 / 平移 / 撤销缩放（Graph Palette） | **#79** 核心 | — |
| ③④ 图例与游标（Plot Legend + Cursor Legend） | **#74** 核心 | #79 的图例行 |
| ⑤ 导出（右键 Export） | **#78** 次级 | — |
| ⑥ 暗扫 J–V 是另一张图 | **#73** | #72 #77 |
| ⑦ 曲线上的读数标注 | **#75** | — |
| ⑧ −4 V 版面 | **#80** | — |
| ⑨ 五条一行能改完的小毛病 | **#64** | #65 #68 #69 #76 |

关闭并折叠进上表的：#65 #68 #69 #71 #72 #76 #77。原文都留在各自 issue 里，图也都还在这个目录。

动手顺序：**#64**（五条小修，先把画布拿回来）→ **#70 → #79 → #74**（三件核心控件，按依赖顺序：
先有可控的轴，缩放才有东西可改；先有轴和缩放，游标才值得做）→ **#73 #75** → **#78** → **#80**。
