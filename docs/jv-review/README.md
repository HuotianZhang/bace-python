# J–V 曲线显示评审 · 证据图

2026-09-06。J–V 图在四种真实工况下渲染出来，量过像素和数值之后拍的标注图。
每张图对应一个 issue；issue 正文就是这张图加一张数据表，判断依据全在图上。

怎么复现：`python -m bace.service --sim --fast --port 8900 --ui ui/`，把 `ui/fixtures/jv_sim.json`
喂给 `jvModel`，在 Chromium 里量 `getBoundingClientRect()`。图上的每个数字都是量出来的，不是估的。

| 图 | 说的是 | issue |
|---|---|---|
| `f1` | J–V 图被一个空的 172 px 列吃掉 33 % 宽度 | |
| `f2` | 轴标着 `\|I\| / A`，脚注却说 `\|J\|` | |
| `f3` | 双向扫描：正扫和回扫在指标表和光标读数里同名 | |
| `f4` | 单光强的双向扫，被描述成一段光强 ramp | |
| `f5` | 同一张卡上，负号有两个字形 | |
| `f6` | 卡片标题写「1 curves」 | |
| `f7` | 纵轴窗口有一半高度是空的 | |
| `f8` | y 有网格线，x 一条都没有；y 也只有 3 条 | |
| `f9` | 暗扫对数图套用了光扫的 2.6:1，设计稿是 1.53:1 | |
| `f10` | 对数 `\|J\|` 把符号毁掉了：过零点变成一串噪声毛刺 | |
| `f11` | 曲线一多，身份就读不出来了 | |
| `f12` | 图上没有 V_oc / J_sc / MPP | |
| `f13` | 没有迟滞指数，也没有扫描速率 | |
| `f14` | 暗扫的指标表是一张空表 | |
| `f15` | 主科学图形是唯一不能导出的那个 | |
| `f16` | 图上没有一样东西是可调的 | |
| `f17` | `ui-rules` §4「−4 V 起点是版面问题」只做到了「没裁」 | |

红 = 现在的行为，绿 = 建议或参照物（设计稿 / 已有能力）。
